# Tasks

## 1. Проект и зависимости

- [ ] 1.1 Создать `src/AutoCAD/CadLens.AutoCAD/CadLens.AutoCAD.csproj` для `net8.0-windows` с NuGet-пакетом `AutoCAD.NET` 25.0.1; проверить успешное восстановление пакета и отсутствие DLL AutoCAD в выходном каталоге сборки.
- [ ] 1.2 Добавить проект в `CadLens.slnx`; проверить, что `dotnet sln CadLens.slnx list` показывает `CadLens.AutoCAD.csproj`.

## 2. Команда CADLENS

- [ ] 2.1 Зарегистрировать документную команду `CADLENS`, выводящую через редактор приветствие с `CAD Lens` без обращения к базе DWG; проверить компиляцию проекта и отсутствие в коде операций изменения чертежа.
- [ ] 2.2 Описать в `README.md` сборку, ручную загрузку через `NETLOAD` и запуск `CADLENS`; проверить, что указанные команды и путь к DLL соответствуют результату сборки.

## 3. Проверка в AutoCAD

- [ ] 3.1 Загрузить DLL через `NETLOAD` в AutoCAD 2026, выполнить `CADLENS` при открытом DWG и зафиксировать версию хоста, приветствие, завершение без запросов и неизменность состояния чертежа.
- [ ] 3.2 Повторить проверку загрузки, приветствия и неизменности DWG в AutoCAD 2025; при отсутствии хоста оставить задачу открытой и явно отметить непроверенную совместимость.
- [ ] 3.3 Выполнить `openspec validate autocad-bootstrap --strict` и сопоставить результаты проверки в хостах со сценариями `specs/autocad-command/spec.md`; проверить, что все требования имеют подтверждённый результат или явно отмеченный пробел.