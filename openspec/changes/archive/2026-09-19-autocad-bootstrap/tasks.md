# Tasks

## 1. Проект и зависимости

- [x] 1.1 Создать `src/AutoCAD/CadLens.AutoCAD/CadLens.AutoCAD.csproj` для `net8.0-windows`; хранить версии и ссылки на пакеты в корневом `Directory.Build.props`, включить `AutoCAD.NET` 25.0.1 через `UseAutocad=true` в `.csproj` и подключить `JetBrains.Annotations` всем проектам; проверить восстановление и отсутствие пакетных DLL в выходном каталоге.
- [x] 1.2 Добавить проект в `CadLens.slnx`; проверить, что `dotnet sln CadLens.slnx list` показывает `CadLens.AutoCAD.csproj`.

## 2. Команда CADLENS

- [x] 2.1 Зарегистрировать документную команду `CADLENS`, выводящую через редактор приветствие с `CAD Lens` без обращения к базе DWG; пометить вызываемые AutoCAD класс и метод `UsedImplicitly`; проверить компиляцию и отсутствие операций изменения чертежа.
- [x] 2.2 Описать в `README.md` сборку, ручную загрузку через `NETLOAD` и запуск `CADLENS`; проверить, что указанные команды и путь к DLL соответствуют результату сборки.

## 3. Проверка в Civil 3D и границы совместимости

- [x] 3.1 Загрузить DLL через `NETLOAD` в Civil 3D 2026 на базе AutoCAD 2026, выполнить `CADLENS` при открытом DWG и зафиксировать версию хоста, приветствие, завершение без запросов и неизменность состояния чертежа.
- [x] 3.2 Явно отметить в `README.md`, что отдельные AutoCAD 2025/2026 не проверены; их установка и проверка не входят в критерий завершения первого среза.
- [x] 3.3 Выполнить `openspec validate autocad-bootstrap --strict` и сопоставить результаты проверки в хостах со сценариями `specs/autocad-command/spec.md`; проверить, что все требования имеют подтверждённый результат или явно отмеченный пробел.