namespace CadLens.Lenses;

/// <summary>An action supported by a presentation node.</summary>
public enum LensAction
{
    /// <summary>Explicitly fit usable target bounds in the active view.</summary>
    Focus
}