using System.Windows.Controls;

namespace CadLens.Preview;

/// <summary>Pairs each drawing action with its automatic navigation setting.</summary>
public partial class ModeActionRows : UserControl
{
    internal ModeActionRows() => InitializeComponent();
}
